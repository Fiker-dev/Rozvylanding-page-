:root{
  --bg:#0b0c10;
  --card:#12141b;
  --muted:#a9b0c3;
  --text:#eef1f7;
  --line:rgba(255,255,255,.08);
  --accent:#6ee7ff;
  --accent2:#9b8cff;
  --shadow: 0 20px 60px rgba(0,0,0,.45);
  --radius:18px;
}
*{box-sizing:border-box}
html,body{margin:0;padding:0}
body{
  font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
  background: radial-gradient(1200px 600px at 20% -10%, rgba(110,231,255,.18), transparent 55%),
              radial-gradient(900px 500px at 90% 10%, rgba(155,140,255,.18), transparent 55%),
              var(--bg);
  color:var(--text);
}
a{color:inherit;text-decoration:none}
.wrap{max-width:1100px;margin:0 auto;padding:18px}
.topbar{
  position:sticky;top:0;z-index:10;
  background: rgba(11,12,16,.72);
  backdrop-filter: blur(10px);
  border-bottom:1px solid var(--line);
}
.topbar__inner{display:flex;align-items:center;justify-content:space-between;gap:16px}
.brand{display:flex;align-items:center;gap:12px}
.brand__mark{
  width:40px;height:40px;border-radius:12px;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  display:grid;place-items:center;
  font-weight:800;color:#06070a;
}
.brand__name{font-weight:800;letter-spacing:.2px}
.brand__sub{font-size:12px;color:var(--muted)}
.cartBtn{
  border:1px solid var(--line);
  background: rgba(255,255,255,.04);
  padding:10px 12px;border-radius:14px;
  color:var(--text);cursor:pointer;
  display:flex;align-items:center;gap:8px;
}
.pill{
  min-width:26px;height:22px;border-radius:999px;
  background: rgba(110,231,255,.18);
  display:grid;place-items:center;
  font-size:12px;font-weight:700;border:1px solid rgba(110,231,255,.28);
}
.hero{display:grid;grid-template-columns: 1.15fr .85fr;gap:18px;align-items:stretch;margin-top:10px}
@media (max-width: 900px){.hero{grid-template-columns:1fr}}
.hero__left{
  border:1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: var(--radius);
  padding:22px;
  box-shadow: var(--shadow);
}
.hero__left h1{margin:0 0 8px;font-size:42px;line-height:1.05}
@media (max-width: 520px){.hero__left h1{font-size:34px}}
.muted{color:var(--muted)}
.tiny{font-size:12px}
.hero__trust{margin-top:14px;display:grid;gap:8px}
.trustItem{
  padding:10px 12px;
  border-radius: 14px;
  border:1px solid var(--line);
  background: rgba(18,20,27,.55);
}
.hero__ctaRow{display:flex;gap:10px;flex-wrap:wrap;margin-top:16px}
.btn{
  border-radius: 14px;
  padding:12px 14px;
  border:1px solid var(--line);
  cursor:pointer;
  font-weight:700;
  background: rgba(255,255,255,.04);
}
.btnPrimary{
  border-color: rgba(110,231,255,.28);
  background: linear-gradient(135deg, rgba(110,231,255,.18), rgba(155,140,255,.18));
}
.btnGhost{background: transparent}
.full{width:100%}

.heroCard{
  border:1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: var(--radius);
  overflow:hidden;
  box-shadow: var(--shadow);
  height:100%;
  display:flex;flex-direction:column;
}
.heroImg{width:100%;height:100%;max-height:340px;object-fit:cover;display:block}
.heroCard__footer{
  display:flex;align-items:flex-end;justify-content:space-between;gap:14px;
  padding:14px 16px;border-top:1px solid var(--line)
}
.priceBig{font-size:22px;font-weight:900}

.section{margin-top:22px}
.section__head{display:flex;align-items:flex-end;justify-content:space-between;gap:12px;margin-bottom:12px}
.section__head h2{margin:0}
.grid{
  display:grid;
  grid-template-columns: repeat(12, 1fr);
  gap:14px;
}
.card{
  grid-column: span 6;
  border:1px solid var(--line);
  background: rgba(18,20,27,.6);
  border-radius: var(--radius);
  overflow:hidden;
}
@media (max-width: 900px){.card{grid-column:span 12}}
.card__img{width:100%;height:220px;object-fit:cover;display:block}
.card__body{padding:14px}
.card__title{font-weight:900;font-size:18px;margin:0 0 6px}
.card__desc{margin:0 0 12px;color:var(--muted);font-size:14px;line-height:1.4}
.rowline{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-top:10px}
.kgRow{display:grid;gap:10px;margin-top:10px}
.kgTop{display:flex;align-items:center;justify-content:space-between}
.range{width:100%}
.kgValue{font-weight:900}
.price{font-weight:900}
.smallBtn{
  width:100%;
  padding:12px 14px;border-radius:14px;
  border:1px solid rgba(110,231,255,.28);
  background: rgba(110,231,255,.12);
  font-weight:900;
  cursor:pointer;
}

.fineprint{
  border:1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: var(--radius);
  padding:16px;
}
.steps{margin:10px 0 0;color:var(--muted);line-height:1.6}

.drawer{position:fixed;inset:0;display:none;z-index:50}
.drawer.open{display:block}
.drawer__overlay{position:absolute;inset:0;background:rgba(0,0,0,.55)}
.drawer__panel{
  position:absolute;top:0;right:0;height:100%;
  width:min(420px, 92vw);
  background: rgba(11,12,16,.92);
  border-left:1px solid var(--line);
  backdrop-filter: blur(10px);
  display:flex;flex-direction:column;
}
.drawer__head{
  padding:14px 14px;
  border-bottom:1px solid var(--line);
  display:flex;align-items:center;justify-content:space-between;gap:10px
}
.drawer__title{font-weight:900}
.iconBtn{
  border:1px solid var(--line);
  background: rgba(255,255,255,.04);
  color:var(--text);
  border-radius:12px;
  width:38px;height:38px;
  cursor:pointer;
}
.drawer__body{padding:12px 14px;overflow:auto;display:grid;gap:10px}
.cartItem{
  border:1px solid var(--line);
  background: rgba(18,20,27,.55);
  border-radius:16px;
  padding:12px;
  display:grid;gap:8px;
}
.cartItem__top{display:flex;justify-content:space-between;gap:10px}
.cartItem__name{font-weight:900}
.cartItem__meta{color:var(--muted);font-size:12px}
.qtyRow{display:flex;gap:8px;align-items:center}
.qtyBtn{
  width:34px;height:34px;border-radius:12px;
  border:1px solid var(--line);
  background: rgba(255,255,255,.04);
  color:var(--text);cursor:pointer;
}
.drawer__footer{padding:12px 14px;border-top:1px solid var(--line);display:grid;gap:10px}
.totals{border:1px solid var(--line);border-radius:16px;padding:12px;background:rgba(18,20,27,.55)}
.totals .row{display:flex;justify-content:space-between;margin:6px 0}
.totals .total{font-weight:900;font-size:16px}
